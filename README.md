# Rahul Mohankumar

Personal website. Static HTML, CSS, and JavaScript hosted with GitHub Pages.

Edit the site files and push to `main` to publish updates.

Run locally with `python3 -m http.server 8766`.
